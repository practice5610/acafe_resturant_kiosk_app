Place the kiosk welcome background image here as:

    kiosk_intro_Image.png

It is shown as the full-screen background of the first (welcome) screen. If the
file is missing, the screen falls back to a solid dark background, so the app
still runs.

Recommended: a portrait (vertical) PNG/JPG at the kiosk's screen resolution.
